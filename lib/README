README
===============================================================================

SliTaz tools, tinyutils, GTKboxes, libraries and functions. On the 
system, all libs go in /usr/lib/slitaz. Some libs have a usage and
can be called directly from the command line, example:

 # /usr/lib/slitaz/libmountbox usage
 
